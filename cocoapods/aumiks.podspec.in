Pod::Spec.new do |s|
  s.name         = "aumiks"
  s.version      = "$(version)"
  s.summary      = "C++ cross-platform audio mixer library."
  s.homepage     = "https://github.com/igagis/aumiks"
  s.license      = 'MIT'
  s.author       = { "Ivan Gagis" => "igagis@gmail.com" }
  s.platform     = :ios, "7.0"
  
  s.source = { :git => "https://github.com/igagis/aumiks.git", :tag => s.version }

  s.source_files  = "src/**/*.{hpp,cpp}"
  s.header_mappings_dir = "src"

  s.dependency 'libaudout', '>= 0.3.2'
  s.dependency 'libpapki', '>= 1.0.9'
end
